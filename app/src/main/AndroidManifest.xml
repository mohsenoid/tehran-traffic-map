<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tehran.traffic"
    android:installLocation="auto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".AnalyticsApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="ir.adad.client.AdadActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:theme="@android:style/Theme.Translucent" />

        <!--<activity-->
        <!--android:name="org.acra.CrashReportDialog"-->
        <!--android:excludeFromRecents="true"-->
        <!--android:finishOnTaskLaunch="true"-->
        <!--android:launchMode="singleInstance"-->
        <!--android:process=":error_report"-->
        <!--android:theme="@style/Theme.Dialog" />-->

        <service android:name=".service.TrafficFirebaseInstanceIdService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <service android:name=".service.TrafficFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notification" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/button" />

        <meta-data
            android:name="AdadToken"
            android:value="d8efa7999202494f80252e5539496795" />

        <meta-data
            android:name="AdadBannersEnabledOnStart"
            android:value="true" />

        <meta-data
            android:name="AdadTestMode"
            android:value="false" />

        <meta-data
            android:name="AdadShowRemoveAds"
            android:value="false" />

        <meta-data
            android:name="AdadLogging"
            android:value="false" />

        <meta-data
            android:name="AdadLoggingLevel"
            android:value="debug" />

    </application>


</manifest>